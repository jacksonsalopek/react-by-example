import { StyledComponents } from "./StyledComponents";

export default {
	title: "Example/StyledComponents",
	component: StyledComponents,
	tags: ["autodocs"],
};

export const Default = {};

export const Toggled = {
	args: {
		isToggled: true,
	},
};
